Implement an Argyll {{language}} step named {{step_name}}.

Requirements:
{{requirements}}

Use the Argyll SDK guidance: prefer SDK-hosted Start/start for POST handlers; use Register/register with WithEndpoint/with_endpoint and WithMethod/with_method for external GET, PUT, or DELETE endpoints. Declare inputs and outputs explicitly and use async context/webhooks for async steps.
